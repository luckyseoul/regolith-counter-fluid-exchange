# Legacy simulators (not production)

These trees are kept for provenance. They are **not** the source of the numbers in the README or the patent evidence package.

RCFX particle-scale evidence is the **custom GPU DEM** in `../custom_gpu_dem/`. LIGGGHTS and YADE were tried first and dropped after contact-model / lost-atom / packing failures. The OpenFOAM/CFDEM case on the Jetson Orin never wrote solver output (empty dumps, ICs only).
